/* Выполнил Колев Георгий Юоьевич
   Назначение: Проверка на целое, если рациональное число является целым, то «да», иначе «нет»
   Возвращаемое значение: bool
   Параметры: рациональное число
bool INT_Q_Z(DM_Q f)

 {

 DM_N os;

 os = GCF_NN_N(ABS_Z_N(f.a), ABS_Z_N(f.b)); //находим наибольший общий делитель числителя и знаменателя, предварительно переведя их из целых чисел в натуральные

 if (COM_NN_D(os, ABS_Z_N(f.b)) == 0) return true; //если числитель полностью делется на знаменаель(т.е наибольший общий делитель равен знаменателю)
                                                      //то возвращаем true
 else return false;//иначе false



 }
